# ============ 构建/版本相关 ============
# 指定版本号 (如 1.4.6)
OPENCODE_VERSION=

# 指定渠道 (如 latest, beta, 或 git 分支名)
OPENCODE_CHANNEL=

# 递增版本类型: major / minor / patch
OPENCODE_BUMP=

# 是否创建 GitHub release: true
OPENCODE_RELEASE=

# GitHub 仓库 (用于 release 上传)
GH_REPO=